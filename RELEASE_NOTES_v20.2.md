# ITTOKAI v20.2 — 電子書類リアルタイム同期改善

## 改善内容
- 施設管理者が書類を送付した後、家族側はログインし直さなくても自動反映します。
- 家族が署名した後、施設管理者側はログインし直さなくても署名済ステータスへ自動更新します。
- 電子書類画面では約3秒ごとに軽量な更新確認のみを行い、変更があった時だけ最新データを再取得します。
- その他の画面では約15秒ごとに更新確認します。
- ブラウザを別タブから戻した時／アプリを前面に戻した時にも即時更新確認します。
- 署名を書いている最中は自動再描画せず、入力中のサインを保護します。
- `/api/documents/revision` を追加し、毎回全データを取得せずに変更有無だけ確認します。
- API GETは `no-store` とし、古いレスポンスのキャッシュ利用を防止します。
- PWAキャッシュを v20.2 に更新しました。

## 期待動作
1. 管理者Aで書類送付。
2. 家族Bはログアウト／再ログイン不要。電子書類画面を開いていれば約3秒以内に表示。
3. 家族Bが署名。
4. 管理者Aは電子書類画面を開いていれば約3秒以内に「署名済」へ更新。
